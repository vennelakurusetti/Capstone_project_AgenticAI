# ============================================================
# Compliance Advisory & Triage Agent — Dependencies
# Pinned for reproducible Streamlit Community Cloud builds.
# ============================================================

# ── CPU-only PyTorch (avoids 2GB GPU wheel on cloud) ────────
# Install torch CPU wheel BEFORE sentence-transformers so pip
# doesn't pull the full CUDA build.
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.3.1+cpu
torchvision==0.18.1+cpu

# Core framework
streamlit==1.35.0
python-dotenv==1.0.1
pydantic==2.7.4
pydantic-settings==2.3.0

# LangChain ecosystem
langchain==0.2.16
langchain-community==0.2.16
langchain-core==0.2.38
langchain-openai==0.1.23
langchain-text-splitters==0.2.2

# LangGraph
langgraph==0.1.19

# Vector store
chromadb==0.5.3
langchain-chroma==0.1.2

# Embeddings — requires torch (CPU wheel installed above)
sentence-transformers==3.0.1

# PDF loading
pypdf==4.2.0
pdfplumber==0.11.0

# OpenAI-compatible client (used for OpenRouter)
openai==1.35.0

# HTTP
httpx==0.27.0
requests==2.32.3

# Data handling
pandas==2.2.2
numpy==1.26.4

# Visualisation
plotly==5.22.0

# Utilities
tenacity==8.4.2
loguru==0.7.2
